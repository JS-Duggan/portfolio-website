---
import { PageMetadata } from "@/src/components/hybrid-astro-ui/page-metadata";

interface Props {
	title: string;
	description: string;
	locale: string;
}

const { title, description, locale } =
	Astro.props as Props;
---

<!doctype html>
<html lang={locale} class="dark">
	<head>
		<PageMetadata
			lang={locale}
			title={title}
			description={description}
			enableClientRouter
			ogType="website"
			favicons={[  { rel: "icon", type: "image/png", href: "/portfolio-website/icon.png", sizes: "32x32" },]}
		/>
	</head>
	<body class="*:max-w-2xl *:w-full flex flex-col  items-center w-full font-mono px-5 md:px-0 leading-7" >
		<slot />
	</body>
</html>
